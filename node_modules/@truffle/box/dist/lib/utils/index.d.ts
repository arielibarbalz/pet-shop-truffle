import type { boxConfig, unboxOptions } from "typings";
declare const _default: {
    downloadBox: (source: string, destination: string, events: any) => Promise<void>;
    readBoxConfig: (destination: string) => Promise<boxConfig>;
    setUpTempDirectory: (events: any) => {
        path: string;
        cleanupCallback: () => void;
    };
    unpackBox: (tempDir: string, destination: string, boxConfig: boxConfig, unpackBoxOptions: unboxOptions) => Promise<void>;
    setUpBox: (boxConfig: boxConfig, destination: string, events: any) => void;
};
export = _default;
